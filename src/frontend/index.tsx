import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App: React.FC = () => {
    return <div>Hello, Chess Opening Master!</div>
};

ReactDOM.render(<App />, document.getElementById('root'));
